import java.util.ArrayList;

/**
 * Created by Bas on 26-6-2016.
 */
public class BinarySearch {
//    returns the index of the word to be searched in an ordered list. Returns -1 if not in list
    public static int Search(String word, int[] orderedList) {
        return 0;
    }

    public static int Search(String word, int[] orderedList, int high, int low) {
        int mid = ((high - low) / 2) + low;
        if (TODO) {

        }
    }
}
